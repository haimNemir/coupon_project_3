#Those files (1- ".env", 2- ".env.production") purpose is to define the variable "REACT_APP_API_URL" globaly. when we in developing mode localy and running the command "npm start" this variable value is what it is saved in file ".env", and when we develping for external server and running the command "npm run build" the value will be from file ".env.production".
REACT_APP_API_URL=http://localhost:8080
